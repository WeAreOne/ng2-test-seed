describe('universal truths', () => {
  it('should do math', () => {
    expect(1 + 1).toEqual(/* CHANGE ME */ 0);

    expect(5).toBeGreaterThan(/* CHANGE ME */ 10);
  });

  xit('should skip this', () => {
    expect(4).toEqual(40);
  });
});
